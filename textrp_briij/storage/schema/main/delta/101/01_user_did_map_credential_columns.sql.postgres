/* Extend user_did_map for XLS-70 credential metadata (Postgres-only). */

ALTER TABLE user_did_map
    ADD COLUMN IF NOT EXISTS credential_id TEXT,
    ADD COLUMN IF NOT EXISTS credential_issued_at BIGINT;
